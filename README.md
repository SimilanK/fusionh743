# fusionh743
H743 The transmit recieve worked. but data got cooked
Connectivity: SPI1 (MS5607-partially worked), SPI4(ICM-haven't touched it, SDMMC1-haven't touch it,I2C:MMC-haven't touch it)
I used SPI2 before but the analog (Px_C) causing a problem to MISO pin so I changed it to the normal looking connection(SPI1 and 4) instead.

